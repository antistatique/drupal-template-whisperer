<?php

/**
 * @file
 * Legacy Drupal 7 code to implements hooks.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function template_whisperer_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the template_whisperer module.
    case 'help.page.template_whisperer':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Adds a field that allows you to choose a template to render the node with. Think Wordpress templates.') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme_suggestions_node_alter().
 */
function template_whisperer_theme_suggestions_node_alter(array &$suggestions, array $variables, $hook) {
  dump($hook);

  // if (isset($variables['elements']['#node']->field_theme_model)
  //     && !empty($variables['elements']['#node']->field_theme_model->entity->field_theme_suggestion->value)
  // ) {
  //     $suggestions[] = 'node' . '__' . $variables['elements']['#node']->getType() . '__' . $variables['elements']['#node']->field_theme_model->entity->field_theme_suggestion->value;
  // }
}
